CREATE TABLE "public"."character" ("rank" integer NOT NULL, "poe_account_name" text NOT NULL, "name" text NOT NULL, "class" text NOT NULL, "level" integer NOT NULL, "experience" integer NOT NULL, "retired" boolean NOT NULL, "dead" boolean NOT NULL, "id" text NOT NULL, "challenges" integer NOT NULL, "twitch" text, PRIMARY KEY ("id") , FOREIGN KEY ("poe_account_name") REFERENCES "public"."user"("poe_name") ON UPDATE no action ON DELETE no action);
