subscription LeagueCharacters {
  character(order_by: { rank: asc }) {
    twitch
    retired
    poe_account_name
    rank
    name
    level
    id
    experience
    dead
    class
    challenges
  }
}
