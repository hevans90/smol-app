query League {
  app_config_by_pk(id: 1) {
    id
    league_name
    league {
      url
      start_at
      register_at
      realm
      id
      end_at
      delve_event
      description
      category_id
    }
  }
}
